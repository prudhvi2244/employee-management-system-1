
spring:
  application:
    name: EMPLOYEE-MANAGEMENT-SYSTEM
  datasource:
    url: jdbc:postgresql://postgresql-service:5432/employeedb
    username: postgres
    password: test1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 9876